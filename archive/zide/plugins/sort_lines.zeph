// Zide plugin: sort the lines of the file alphabetically.
let text = read_file(args()[1])
var lines = text.split("\n")
lines.sort()
var out: [str] = []
var i = 0
while i < lines.len() {
    out.push(lines[i])
    if i < lines.len() - 1 { out.push("\n") }
    i += 1
}
emit(out.join())
